use crate::aes::*;
use crate::galoafield::GF2_8;

/*
pub fn dfa(s: &State, broken: &State) -> Vec<GF2_8> {
    let mut diff = add_state(s, broken);
    let mut diff = add_state(inv_sub_byte(s), inv_sub_byte(s_broken));
    for eps in s_diff.iter().flatten().filter(|&v| v.0 != 0) {
        for c in 1..4 {

        }
    }
}
*/
